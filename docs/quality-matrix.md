# Quality contract matrix

This matrix turns the SQLite-inspired requirements lesson into a small, repository-local traceability map.

It links a behavior claim to implementation evidence and a deterministic check.
It is not a coverage percentage, a claim of SQLite-level quality, or a list of every test.

## How to read the matrix

Each identifier is local to txBASE.

`Current` means the contract is implemented and represented by the cited check.

`Boundary` means the behavior is intentionally limited and the limit is part of the contract.

`Future` means the topic is documented but must not be described as an implemented feature.

The authoritative CI command is:

```bash
bash scripts/check-doc-translations.sh
cargo fmt --all -- --check
cargo clippy --all-targets --all-features -- -D warnings
cargo build --bin txbase
bash tests/e2e/cli_lifecycle.sh
cargo test --all-targets --all-features
```

The native workflow runs that gate on Ubuntu, macOS, and Windows.
The separate Ubuntu WASM job runs `cargo build --locked --lib --target wasm32-unknown-unknown --release`, generates the Node.js wrapper with `wasm-bindgen-cli` `0.2.128`, and runs `node tests/wasm_smoke.mjs target/wasm-bindgen` and `node tests/wasm_edge_smoke.mjs target/wasm-bindgen`.

## Current contracts

| ID | Contract | Implementation or fixture evidence | Deterministic check | Status |
| --- | --- | --- | --- | --- |
| QRY-003A | `$sortByCount` evaluates the shared bounded scalar-expression subset per input record, groups missing or null results as `null`, and retains the 10,000-group bound. | `src/query/aggregation_plan/stage_parsers.rs`; `src/query/aggregation/output.rs`; `src/query/aggregation_tests/sort_by_count_tests.rs`; `docs/aggregation.md` | `sort_by_count_evaluates_a_scalar_expression_per_record` | Boundary |
| DBF-001 | Declared headers, descriptors, record lengths, and deletion markers are bounds-checked. | `src/dbf/parser.rs`; `tests/corpus/dbf/` | `src/dbf/malformed_tests.rs::rejects_malformed_dbf_corpus` and format tests | Current |
| DBF-002 | Supported dBASE III, dBASE IV, and Visual FoxPro fixtures, including an upstream Windows-1251 table, round-trip through the declared field boundary and expose the declared code-page name in schema metadata. | `tests/fixtures/external-*.dbf.hex`; `src/dbf/compatibility_tests.rs`; `src/dbf/codec.rs`; `src/dbf/encoding_name_tests.rs` | `reads_and_writes_a_pinned_external_*_fixture`; `reads_and_writes_a_pinned_external_cp1251_fixture`; `reports_names_for_supported_language_drivers` | Current |
| DBF-003 | The four Visual FoxPro CJK driver IDs, the legacy dBASE aliases `0x13`, `0x4d`, `0x4e`, and `0x4f`, and all eight supported explicit codec names use byte-width validation, replacement reads, rejecting writes, visible declared/effective metadata, and effective-codec field-name decoding. | `src/dbf/codec_cjk.rs`; `src/dbf/codec_fields.rs`; `src/dbf/schema.rs`; `src/dbf/cjk_tests.rs`; `src/dbf/cjk_fields_tests.rs`; `src/dbf/upstream_cjk_tests.rs`; `src/dbf/schema_encoding_tests.rs`; `tests/fixtures/cjk-*.dbf.hex`; `tests/fixtures/cjk-field-name-gbk.dbf.hex`; `tests/fixtures/external-javadbf-gbk.dbf.hex`; `tests/fixtures/external-dbase-rs-cp936.dbf.hex`; `tests/fixtures/external-korea-maps-euc-kr.dbf.hex`; `tests/fixtures/external-korea-maps-city-euc-kr.dbf.hex`; `tests/fixtures/cjk-explicit-codecs.json` | `reads_and_writes_pinned_cjk_driver_fixtures`; `decodes_and_encodes_declared_cjk_drivers`; `decodes_and_encodes_legacy_cjk_driver_aliases`; `decodes_cjk_field_names_with_the_effective_encoding`; `reads_and_writes_a_pinned_upstream_gbk_fixture`; `reads_and_writes_a_pinned_upstream_cp936_fixture`; `reads_and_writes_a_pinned_upstream_euc_kr_fixture`; `reads_and_writes_a_pinned_upstream_city_euc_kr_fixture`; `reads_pinned_explicit_cjk_codec_bytes_from_a_dbf_record`; `explicit_euc_jp_and_gb18030_overrides_round_trip`; `explicit_iso_2022_jp_override_round_trips_jis_text`; `strict_shift_jis_override_round_trips_jis_text`; `strict_shift_jis_rejects_cp932_extensions`; `accepts_strict_shift_jis_as_an_explicit_override`; `accepts_iso_2022_jp_as_an_explicit_override`; `recovers_an_operation_with_a_path_encoding_override` | Current |
| MEM-001 | DBT and FPT pointers, block sizes, terminators, and malformed sidecars are handled within the selected memo format. | `src/dbf/memo_file.rs`; `src/dbf/memo_value.rs`; `tests/corpus/memo/` | `src/dbf/tests/memo.rs`, `memo_foxpro.rs`, and `malformed_memo_tests.rs` | Current |
| MUT-001 | Record mutations preserve the in-memory model, reject unknown or ambiguous writes, and keep opaque fields intact. | `src/dbf/mutation.rs`; `src/dbf/mutation_update.rs`; `src/dbf/mutation_model_tests.rs` | `generated_mutation_sequence_matches_reference_model` and mutation tests | Current |
| WAL-001 | Complete WAL records are validated; torn final records are truncated or discarded without a panic. | `src/transaction/wal.rs`; `src/dbf/wal.rs`; `tests/corpus/wal/` | `transaction::malformed_tests::rejects_malformed_wal_corpus`; `file_wal_reopens_and_truncates_a_torn_tail` | Current |
| WAL-002 | Snapshot, delta, mutation-intent, memo, and index recovery are idempotent across replacement boundaries, while malformed index snapshots remain recoverable WAL errors. | `src/dbf/recovery.rs`; `src/dbf/persistence.rs` | `src/dbf/tests/recovery.rs` recovery cases; `recovery_fault_tests.rs::keeps_a_wal_with_a_malformed_index_snapshot` | Current |
| WAL-003 | Independently loaded stale writers are rejected rather than silently overwriting newer DBF or memo bytes. | `src/dbf/persistence.rs`; `src/dbf/tests/persistence.rs`; `src/dbf/writer_tests.rs` | `rejects_a_stale_second_writer_without_overwriting_the_first_save`; `rejects_stale_memo_sidecar_before_save`; `rejects_a_new_memo_sidecar_before_save` | Current |
| WAL-004 | Read-only WAL inspection reports complete record boundaries and an incomplete final tail without creating, truncating, or mutating the WAL; complete malformed records remain errors. | `src/transaction/wal.rs`; `src/cli.rs`; `tests/cli.rs`; `docs/dbf-compatibility.md` | `file_wal_inspect_reports_but_does_not_truncate_a_torn_tail`; `file_wal_inspect_does_not_create_a_missing_file`; `wal_inspect_cli_reports_a_torn_tail_without_mutating_the_file` | Current |
| TXN-001 | The low-level snapshot transaction manager resumes the next transaction ID from retained memory or file WAL commit/rollback records without claiming DBF/catalog HTTP IDs or MVCC visibility. | `src/transaction/mod.rs`; `src/transaction/wal.rs` | `snapshot_engine_resumes_transaction_ids_from_wal`; `file_snapshot_engine_resumes_transaction_ids_after_reopen` | Boundary |
| TXN-002 | Single-table DBF WAL commits allocate positive durable IDs, recover them from `TXTI`, reject stale transaction-state writers, copy the state sidecar, and expose the ID on HTTP mutations. | `src/dbf/persistence.rs`; `src/dbf/recovery.rs`; `src/dbf/maintenance.rs`; `src/server/etag.rs` | `wal_commit_ids_persist_and_resume_after_reload`; `snapshot_recovery_persists_the_wal_transaction_id`; `copy_table_files_preserves_transaction_state`; `mutation_etag_prevents_lost_update` | Current |
| TXN-003 | `DbfTransaction` loads a path-backed snapshot, keeps mutations and queries private, commits all applied operations through one WAL-backed save, rejects a stale DBF source without overwriting a concurrent commit, and rolls back without touching the path; HTTP `POST /transaction` reuses this boundary. | `src/dbf/transaction.rs`; `src/dbf/transaction_tests.rs`; `src/server/transaction.rs`; `docs/transactions.md`; `docs/mvcc.md`; `docs/http-semantics.md` | `snapshot_transaction_queries_private_changes_and_commits_once`; `snapshot_transaction_rejects_a_concurrent_commit_without_overwriting_it`; `rollback_discards_the_private_snapshot`; existing HTTP transaction tests | Boundary |
| TXN-004 | Explicit `DbfTransaction::commit_with_row_merge` reloads the current image under the table lock and merges only disjoint physical-row updates or deletes with unchanged schema, layout, and record count; inserts, different changes to the same row, and layout changes remain errors, while an identical resulting row is a no-op, and the merged result uses the normal WAL/MVCC/sidecar path. | `src/dbf/persistence.rs`; `src/dbf/transaction.rs`; `src/dbf/transaction_tests.rs`; `docs/transactions.md`; `docs/mvcc.md` | `snapshot_transaction_merges_disjoint_row_changes_when_explicitly_requested`; `snapshot_transaction_merges_a_disjoint_row_delete_when_explicitly_requested`; `snapshot_transaction_rejects_an_insert_during_row_merge`; `snapshot_transaction_rejects_a_same_row_merge_conflict` | Boundary |
| TXN-005 | `DbfTransaction::begin_serializable` acquires and holds the exclusive table lock from begin through commit or rollback, commits through the already-held lock without reentrant acquisition, and leaves the lock available after rollback. The boundary is one-table and coarse-grained; predicate-level semantics remain future work. | `src/dbf/lock.rs`; `src/dbf/persistence.rs`; `src/dbf/transaction.rs`; `src/dbf/transaction_tests.rs`; `docs/transactions.md`; `docs/mvcc.md` | `serializable_transaction_holds_and_releases_the_exclusive_table_lock`; `serializable_transaction_commits_through_the_held_lock` | Boundary |
| MVCC-001 | Single-table DBF commits retain durable prepare and commit records with DBF, memo, and schema images; exact committed snapshots are readable through the library and CLI, while historical tables are read-only. | `src/dbf/mvcc.rs`; `src/dbf/persistence.rs`; `src/dbf/recovery.rs`; `src/cli/commands/mvcc.rs`; `docs/mvcc.md` | `persistent_snapshots_keep_each_committed_table_image`; `tests/e2e/cli_lifecycle.sh` | Current |
| MVCC-002 | Successful catalog-journal commits retain one full image of every discovered table in a versioned `.txbase.catalog.mvcc` history; `Catalog::from_path_at` and the catalog MVCC CLI read one consistent commit-level snapshot, and historical catalogs are read-only. | `src/catalog/mvcc.rs`; `src/catalog/transaction.rs`; `src/catalog/journal.rs`; `src/catalog.rs`; `src/cli/commands/mvcc.rs`; `docs/mvcc.md` | `catalog_mvcc_preserves_consistent_cross_table_snapshots`; `catalog_history_round_trips_binary_table_images`; `prepared_journal_rolls_back_partial_catalog_commit`; `committed_journal_replays_the_complete_catalog_commit` | Boundary |
| MVCC-003 | Table and catalog MVCC GC take the write lock, recover pending state, retain the newest positive count of full-image snapshots, replace only the history sidecar through a synced temporary file, reject zero, and make removed IDs unavailable without changing current data. | `src/dbf/mvcc.rs`; `src/catalog/mvcc.rs`; `src/catalog.rs`; `src/cli/commands/mvcc.rs`; `tests/e2e/cli_lifecycle.sh`; `docs/mvcc.md` | `gc_retains_latest_table_snapshots_and_rejects_zero`; `catalog_mvcc_gc_retains_latest_commits_and_allows_future_appends`; `bash tests/e2e/cli_lifecycle.sh` | Boundary |
| MVCC-004 | Table MVCC stores row changes in the same durable prepare/commit records, exposes retained row history and committed-snapshot row reads, separates physical record identities with epochs after layout changes, and rebuilds a row-history baseline during full-image GC. | `src/dbf/mvcc.rs`; `src/dbf/row_mvcc.rs`; `src/dbf/types.rs`; `src/dbf/persistence.rs`; `src/dbf/maintenance.rs`; `src/dbf/wal.rs`; `src/cli/commands/mvcc.rs`; `tests/e2e/cli_lifecycle.sh`; `docs/mvcc.md`; `docs/dbf-compatibility.md` | `row_mvcc_retains_independent_older_row_versions_and_compacts_with_gc`; `row_mvcc_separates_physical_record_ids_after_pack`; `bash tests/e2e/cli_lifecycle.sh` | Boundary |
| MVCC-005 | Table MVCC GC can independently retain up to a positive count of older versions per physical row before the oldest retained full snapshot, stores those detached versions in the existing history sidecar, keeps full-image GC behavior unchanged, and allows `mvcc row` and `mvcc row-at` to read the retained row history without changing current DBF bytes. | `src/dbf/mvcc.rs`; `src/dbf/row_mvcc.rs`; `src/cli/commands/mvcc.rs`; `src/dbf/mvcc_tests.rs`; `docs/mvcc.md`; `docs/cli.md`; `docs/dbf-compatibility.md` | `row_mvcc_retains_independent_older_row_versions_and_compacts_with_gc`; CLI option is covered by the existing e2e build path | Boundary |
| EDGE-001 | The local XBF object-store boundary applies configured XBF limits before publication and during reads, writes immutable snapshots and pending WAL objects, publishes one versioned manifest through compare-and-swap, rejects mixed generations, recovers interrupted publication, supports idempotent retry, persists through a filesystem backend, and removes stale unreferenced objects without requiring a cloud service. | `src/edge/protocol.rs`; `src/edge/table.rs`; `src/edge/store.rs`; `src/edge/memory.rs`; `src/edge/filesystem.rs`; `src/xbf/`; `docs/edge-storage.md` | `commits_and_reads_one_consistent_xbf_generation`; `object_table_enforces_encode_limits_before_publication`; `compare_and_swap_rejects_concurrent_writers_and_allows_idempotent_retry`; `rejects_a_manifest_that_points_to_a_different_snapshot_generation`; `recovers_after_snapshot_and_wal_publication_before_manifest_cas`; `filesystem_store_persists_objects_and_rejects_unsafe_keys`; `filesystem_object_table_reopens_and_recovers_an_interrupted_commit` | Boundary |
| EDGE-002 | The local object-table manifest records committed generation history, `read_at` reads a retained immutable generation without mixing snapshots, and explicit count-based retention atomically compacts the manifest before removing older snapshot objects; cloud-specific retention remains outside the boundary. | `src/edge/protocol.rs`; `src/edge/table.rs`; `src/edge/tests.rs`; `docs/edge-storage.md` | `commits_and_reads_one_consistent_xbf_generation` checks history, historical read, orphan cleanup, and retention; `filesystem_object_table_persists_history_and_retention` checks reopen and retention persistence | Boundary |
| EDGE-003 | The runtime-neutral `AsyncObjectStore` contract covers `get`, `put_if_absent`, compare-and-swap, delete, and list without selecting an executor; the synchronous adapter preserves the existing object-store errors and operation semantics. | `src/edge/store.rs`; `src/edge/memory.rs`; `src/edge/tests.rs`; `docs/edge-storage.md` | `async_adapter_preserves_object_store_operations_and_errors` | Boundary |
| EDGE-004 | `AsyncObjectTable` reuses the synchronous manifest protocol for bounded commit encoding, read, historical read, recovery, retention, and orphan cleanup through `AsyncObjectStore`, including generation checks and conditional publication. | `src/edge/object_store_async.rs`; `src/edge/protocol.rs`; `src/edge/table.rs`; `src/edge/store.rs`; `src/edge/tests.rs`; `docs/edge-storage.md` | `async_object_table_reuses_generation_commit_and_retention_contracts`; `async_object_table_enforces_encode_limits_before_publication`; `async_object_table_recovers_after_manifest_publication_failure` | Boundary |
| WASM-001 | The host-independent WASM core opens DBF bytes, executes the shared bounded query contract, applies the shared `POST`, `PUT`, `PATCH`, and `DELETE` operation contract either singly or as an atomic batch, enforces the shared 1,000-operation batch limit before mutation, returns a new DBF snapshot, and exposes the same boundary through `wasm-bindgen`; it does not provide persistence or host scheduling. | `src/wasm.rs`; `src/xbase.rs`; `src/dbf/operation.rs`; `src/dbf/parser.rs`; `src/query.rs`; `src/dbf/mutation.rs`; `.github/workflows/ci.yml`; `tests/wasm_smoke.mjs`; `docs/wasm.md` | `wasm_core_reuses_query_and_mutation_contracts`; `wasm_core_batch_mutation_commits_all_operations`; `wasm_core_batch_mutation_is_atomic_on_error`; `wasm_core_rejects_empty_operation_batches`; `wasm_core_rejects_oversized_operation_batches_without_mutation`; `operation_batch_rejects_more_than_the_shared_limit`; `applies_all_mutation_methods_through_one_contract`; `rejects_invalid_operation_shapes_without_mutating_the_table`; `node tests/wasm_smoke.mjs target/wasm-bindgen` | Boundary |
| CDC-001 | A single-table DBF commit emits an ordered `TXCD` state-diff event, stages it in the DBF WAL, publishes it after DBF, index, state, and MVCC recovery, repairs a torn CDC tail, rejects conflicting or out-of-order reuse, and copies it in backup and restore. | `src/dbf/cdc.rs`; `src/dbf/persistence.rs`; `src/dbf/recovery.rs`; `src/dbf/maintenance.rs`; `src/dbf/cdc_tests.rs`; `src/dbf/tests/maintenance.rs`; `tests/cli.rs`; `docs/change-data-capture.md` | `cdc_records_committed_row_diffs_in_transaction_order`; `recovery_publishes_a_pending_cdc_event_after_snapshot_replacement`; `cdc_append_is_idempotent_and_rejects_conflicting_reuse`; `cdc_reader_repairs_a_torn_final_record`; `copy_table_files_preserves_change_events`; `cdc_cli_reads_committed_events_and_supports_an_after_cursor` | Boundary |
| CDC-002 | An explicit multi-table catalog commit emits one ordered `TXCC` envelope, journals its publication with DBF, index, MVCC, and transaction-state targets, replays or rolls it back with catalog-journal recovery, rejects conflicting or out-of-order reuse, and exposes an exclusive transaction-ID cursor. | `src/catalog/cdc.rs`; `src/catalog/transaction.rs`; `src/catalog/journal.rs`; `src/catalog/tests.rs`; `src/cli/commands/cdc.rs`; `tests/cli.rs`; `docs/change-data-capture.md` | `catalog_cdc_records_one_atomic_event_for_changed_tables`; `prepared_journal_rolls_back_partial_catalog_commit`; `committed_journal_replays_the_complete_catalog_commit`; `catalog_cdc_cli_reads_an_empty_catalog_stream` | Boundary |
| REPL-001 | A process-local fixed-term authority creates versioned `ReplicationEntry` values and applies them through the existing atomic catalog journal; the journal persists the matching `TXRP` sidecar image with the catalog mutation. Followers require the next term/index/transaction position and the current catalog representation tag, acknowledge exact duplicate delivery, and reject gaps, conflicting duplicates, and mismatches before a new commit. `ReplicationLog::open` validates the sidecar term and catalog position before restart continuation; transport, quorum, and consensus remain outside the boundary. | `src/replication.rs`; `src/replication_tests.rs`; `src/catalog/transaction.rs`; `docs/distributed-evolution.md` | `entries_are_versioned_and_round_trip`; `authority_and_follower_commit_the_same_atomic_entry`; `a_partitioned_follower_rejects_gaps_until_recovery`; `serialized_log_can_resume_after_restart`; `txrp_sidecar_reopens_and_continues_with_the_catalog`; `stale_txrp_sidecar_rejects_a_new_catalog_commit`; `malformed_txrp_sidecar_is_rejected_before_replay`; `schema_and_term_mismatches_are_rejected_before_commit`; `conflicting_duplicate_is_rejected_without_a_second_commit` | Boundary |
| REPL-002 | The local replication log serves a retained catalog MVCC image only at a positive transaction covered by the applied log and only when the live catalog position matches the log; reads beyond the applied position, before the log base, or against a divergent catalog are rejected. The boundary provides historical reads, not network freshness, leases, linearizability, quorum, or consensus. | `src/replication.rs`; `src/replication_tests.rs`; `docs/distributed-evolution.md` | `local_follower_reads_are_limited_to_the_applied_log`; `follower_reads_reject_a_log_and_catalog_position_mismatch` | Boundary |
| REPL-003 | A versioned `ReplicationSnapshot` validates its term, log position, catalog transaction, schema tag, and single-image payload; `snapshot()` exports one current catalog image under the catalog read lock; `install_snapshot()` atomically replaces DBF, memo, schema, catalog MVCC, CDC, and supported replication sidecars, compacts history to the installed base, clears stale table-local state, resumes at the next index and transaction, acknowledges the exact image, and rejects stale, conflicting, divergent, or racing installs. Networked snapshot transport and authority-coordinated truncation remain outside the boundary. | `src/replication/snapshot.rs`; `src/catalog/snapshot.rs`; `src/catalog/mvcc.rs`; `src/catalog/journal.rs`; `src/replication_tests.rs`; `docs/distributed-evolution.md` | `replication_snapshot_round_trips_and_validates_the_catalog_image`; `snapshot_installation_compacts_history_and_resumes_replication`; `snapshot_installation_rejects_stale_and_conflicting_images` | Boundary |
| REPL-004 | The catalog server exposes a version 1 bounded JSON delivery surface for replication status, snapshot export, entry delivery, and snapshot installation. Entry requests use the shared 1 MiB JSON input bound; snapshot requests use the 64 MiB encoded-payload bound; malformed documents return `422`, replication conflicts return `409`, and storage failures return `500`. The default `authority` role captures `/transaction` and named-table mutation routes in the same catalog journal commit as `TXRP` state and rechecks table ETags before commit; the `follower` role returns `409` for direct catalog mutations while replication delivery remains available. When `TXBASE_REPLICATION_TOKEN` is configured, all four replication routes require RFC 6750 `Authorization: Bearer` credentials and return `401` with `WWW-Authenticate: Bearer` when credentials are missing or invalid. | `src/server/replication.rs`; `src/server/catalog.rs`; `src/server/catalog_transaction.rs`; `src/server/catalog_mutation.rs`; `src/server/body.rs`; `src/server/replication_tests.rs`; `docs/distributed-evolution.md`; `docs/http-semantics.md` | `replication_http_delivers_entries_idempotently_and_reports_status`; `replication_http_installs_and_exports_snapshots`; `replication_http_rejects_unknown_entry_fields_before_commit`; `replication_http_enforces_configured_bearer_token`; `authority_captures_catalog_and_named_mutations_in_one_replication_log`; `authority_named_mutations_recheck_table_etags_before_commit`; `follower_role_reports_read_only_mutations` | Boundary |
| CLI-001 | Backup and restore recover pending destination and source state, validate a DBF source, and copy the DBF plus supported sidecars through the sidecar-aware maintenance boundary while holding source and destination table locks in stable order. A source without a sidecar removes the corresponding stale destination sidecar. | `src/cli.rs`; `src/dbf/maintenance.rs`; `src/dbf/tests/maintenance.rs`; `tests/cli.rs`; `docs/dbf-compatibility.md` | `backup_and_restore_cli_copy_a_dbf`; `copy_table_files_preserves_dbf_and_memo_sidecar`; `copy_table_files_preserves_schema_and_replaces_a_stale_destination_schema`; `copy_table_files_recovers_a_pending_destination_wal_before_replacement`; `copy_table_files_preserves_transaction_state`; `copy_table_files_preserves_change_events`; `copy_table_files_preserves_a_valid_index_sidecar`; `copy_table_files_removes_all_stale_destination_sidecars_when_source_has_none` | Current |
| CLI-002 | Schema, verify, pack, and recall commands parse their CLI arguments and operate on a validated DBF through the public binary path; verify also validates any `.txidx` sidecar, and pack compacts referenced DBT/FPT blocks while persisting the DBF and memo snapshot through one WAL boundary. | `src/cli.rs`; `src/dbf/parser.rs`; `src/dbf/maintenance.rs`; `src/dbf/mutation_memo.rs`; `src/dbf/persistence.rs`; `src/index.rs`; `tests/cli.rs`; `src/dbf/tests/memo.rs`; `src/dbf/tests/memo_foxpro.rs`; `docs/dbf-compatibility.md` | `dbf_maintenance_cli_commands_operate_on_a_dbf`; `verify_cli_rejects_a_stale_index_sidecar`; `pack_compacts_dbase3_memo_blocks_and_updates_pointers`; `pack_compacts_foxpro_binary_memo_blocks_and_updates_pointers` | Current |
| CLI-003 | XBF import and representability reporting operate through the public binary path; direct export rejects a non-null constraint that DBF cannot preserve, while schema-preserving export retains the active DBF records. | `src/cli.rs`; `src/xbf/`; `src/dbf/schema_export.rs`; `tests/cli.rs`; `docs/xbf.md` | `xbf_cli_import_report_and_export_a_dbf` | Current |
| CLI-004 | The multi-call CLI initializes a DBF, inserts records through the normal WAL path, reads the current table, lists and reads an MVCC snapshot, and verifies the result in a numbered shell E2E. | `src/cli.rs`; `src/cli/commands/setup.rs`; `src/cli/commands/mvcc.rs`; `tests/e2e/cli_lifecycle.sh`; `.github/workflows/ci.yml` | `bash tests/e2e/cli_lifecycle.sh` on the Ubuntu, macOS, and Windows matrix | Current |
| XBF-001 | The draft XBF codec bounds allocations, validates header and section CRC-32C values, decodes all non-reserved v1 scalar types, preserves physical deletion flags, and enforces local uniqueness. | `src/xbf/`; `src/xbf/malformed_tests.rs`; `tests/corpus/xbf/`; `docs/xbf.md` | `src/xbf/tests.rs` fixture, corruption, size-limit, and constraint cases; `rejects_malformed_xbf_header_corpus`; `rejects_malformed_xbf_sections_and_directory`; `rejects_malformed_schema_and_directory_metadata`; `rejects_malformed_xbf_utf8_and_payload_values`; `rejects_malformed_null_fixed_width_and_json_values` | Boundary |
| XBF-002 | The XBF snapshot path encodes before writing, serializes cooperating reads and writes through `*.txbase.lock`, recovers a pending WAL before direct replacement, syncs the snapshot bytes, replaces the target, and syncs the parent directory before returning. | `src/xbf/persistence.rs`; `src/xbf/wal.rs` | `writes_and_reads_a_durable_snapshot_path`; `reading_a_snapshot_recovers_a_pending_wal`; `direct_snapshot_write_recovers_a_pending_wal_before_replacing_it`; `direct_snapshot_write_preserves_a_generation_conflict` | Boundary |
| XBF-003 | The full-snapshot XBF WAL names its base and target generations, enforces the transaction-layer record limit, applies a pending snapshot idempotently with the caller's limits, and rejects a different current generation. | `src/xbf/wal.rs` | `recovers_a_generation_checked_full_snapshot_wal`; `rejects_a_generation_mismatched_xbf_wal`; `rejects_xbf_wal_records_over_the_file_wal_limit` | Boundary |
| XBF-004 | Loaded DBF records convert to bounded XBF values without dropping physical order, deletion flags, DBF-distinguishable nulls, declared DBF nullability, or representable field-level schema constraints; unsupported values and schema metadata fail explicitly. | `src/xbf/conversion.rs`; `src/dbf/types.rs`; `src/dbf/schema_metadata.rs`; `src/xbf/export_tests.rs` | `converts_a_dbf_fixture_to_xbf_without_dropping_records`; `preserves_declared_dbf_nullability_without_a_current_null_value`; `converts_representable_dbf_schema_constraints_to_xbf`; `rejects_dbf_schema_constraints_without_an_xbf_v1_representation` | Boundary |
| XBF-005 | A representable XBF table exports to an in-memory DBF table with deletion state preserved; direct export rejects unsupported types and constraints, including non-nullability, while schema-aware export returns and can persist representable constraint metadata. Export also rejects invalid primary-key schema flags before producing DBF or sidecar output. | `src/xbf/export/mod.rs`; `src/xbf/export/convert.rs`; `src/xbf/schema.rs`; `src/xbf/export_tests.rs` | `exports_a_representable_xbf_table_to_dbf`; `exports_xbf_constraints_as_schema_metadata`; `rejects_direct_export_of_non_nullable_fields`; `rejects_invalid_primary_key_schema_during_export`; `saves_xbf_dbf_and_schema_sidecar`; `rejects_nonrepresentable_xbf_dbf_export_types` | Boundary |
| XBF-006 | XBF-to-DBF representability can be inspected without writing files, including all discovered field/record issues and schema-sidecar requirement. | `src/xbf/export/mod.rs`; `src/xbf/export/convert.rs`; `src/xbf/export_tests.rs`; `docs/xbf.md` | `reports_all_nonrepresentable_xbf_fields_and_values`; `report_marks_constraint_sidecar_requirement` | Boundary |
| XBF-007 | Schema-preserving XBF-to-DBF file export uses a durable `TXSE` journal, exact target base bytes, all-target preflight and per-target recheck before replacement, per-file sync-and-replace, memo-sidecar cleanup, durable transaction-state replacement, existing-index staging and validation, DBF-read recovery after partial replacement, conflict rejection for externally changed targets, and malformed-journal rejection. | `src/dbf/schema_export.rs`; `src/dbf/persistence.rs`; `src/dbf/parser.rs`; `src/index/commit.rs`; `src/dbf/schema_export_tests.rs` | `schema_export_commits_dbf_and_schema_together`; `schema_export_removes_stale_memo_sidecars`; `schema_export_read_recovers_after_dbf_replacement`; `schema_export_recovery_replays_index_target`; `schema_export_read_rejects_an_external_target_change`; `schema_export_recovery_preflights_every_target_before_replacement`; `schema_export_rejects_malformed_journal_records` | Boundary |
| IDX-001 | External scalar and compound indexes are rebuildable, freshness-checked, rejected before an unsafe mutation, and refreshed after supported mutations or recovery. | `src/index.rs`; `src/index/`; `src/index_tests.rs`; `src/dbf/tests/persistence.rs`; `src/dbf/tests/recovery.rs` | `mutation_refreshes_the_sidecar_after_save`; `rejects_an_invalid_index_before_saving_the_dbf`; `replays_operation_intent_when_state_payload_is_missing`; `direct_dbf_change_leaves_the_sidecar_stale_until_rebuild` | Current |
| IDX-002 | Index candidates preserve table-scan results for scalar equality, compound equality, compound equality-prefix, range, compound equality-prefix range, ordered-prefix, compound, and equality-intersection plans. | `src/query/planner.rs`; `src/query/planner_tests.rs`; `src/query/planner_compound_tests.rs` | `uses_a_compound_index_for_exact_equality`; `uses_a_compound_index_for_an_equality_prefix`; `uses_a_compound_index_for_a_multi_field_equality_prefix`; `uses_a_compound_range_after_an_equality_prefix`; planner tests named `uses_*` and `orders_*` | Current |
| IDX-003 | The selected table-scan or index plan is available as a tagged JSON explanation without changing query execution. | `src/query/planner.rs`; `src/server/explain.rs`; `docs/query-planning.md` | `explain_endpoint_reports_scan_and_index_plans` | Boundary |
| IDX-004 | When equality, range, and ordered access paths coexist, the planner compares bounded candidate-row, index-traversal, logical-page, and sort costs, reports deterministic row-equivalent candidate-read and filter-evaluation fields, falls back to a table scan for an exact non-selective tie, and preserves table-scan results. | `src/query/planner.rs`; `src/query/planner_cost.rs`; `src/query/planner_compound_tests.rs`; `src/query/planner_tests.rs`; `src/index_tests.rs`; `docs/indexes.md` | `chooses_the_access_path_with_fewer_exact_candidates`; `chooses_the_lowest_cost_equality_candidate`; `chooses_the_lowest_cost_range_candidate`; `chooses_a_table_scan_for_a_non_selective_index`; `builds_and_loads_an_external_scalar_index` | Boundary |
| IDX-005 | A valid-index `QUERY /explain` response exposes deterministic row-equivalent `candidate_rows`, `index_traversal`, `index_page_reads`, `record_reads`, `record_page_reads`, `filter_evaluations`, `sort_work`, and `total` costs; missing or invalid sidecars omit the cost and retain the table-scan fallback. | `src/query/planner.rs`; `src/query/planner_cost.rs`; `src/query.rs`; `src/server/explain.rs`; `docs/query-planning.md` | `explain_endpoint_reports_scan_and_index_plans`; planner explanation page-read assertions | Boundary |
| QRY-001 | Query parsing validates the documented filter, path, projection, sort, update, and bounded array-predicate boundaries without claiming MongoDB compatibility. | `src/query/validation.rs`; `src/query/predicate.rs`; `src/query/array_predicate.rs`; `src/query/tests.rs`; `src/query/array_predicate_tests.rs` | `parses_query_shape`; `rejects_unknown_query_fields`; `rejects_unknown_and_mixed_projection_operators`; `matches_all_and_exact_array_size`; `elem_match_binds_all_conditions_to_one_array_element`; `rejects_malformed_array_predicates` | Current |
| QRY-002 | Bounded `$expr` boolean trees over comparison leaves, shared scalar `$literal`/`$ifNull`/`$concat`/`$toLower`/`$toUpper` expressions, unary numeric `$abs`, and two-operand numeric `$add`/`$subtract`/`$multiply`/`$divide`/`$mod` expressions, plus malformed JSON query corpora, fail safely and do not become index lookups. | `src/query/expression.rs`; `src/query/expression/numeric.rs`; `src/query/field_expression_tests.rs`; `src/query/validation.rs`; `src/query/malformed_tests.rs` | `compares_two_fields_with_expr`; `composes_expression_comparisons_with_boolean_operators`; `compares_string_scalar_expression_results`; `string_scalar_expressions_support_null_fallback_and_literal_values`; `compares_numeric_expression_results`; `compares_absolute_expression_results`; `compares_multiplication_expression_results`; `compares_division_expression_results`; `compares_modulo_expression_results`; `rejects_division_by_zero`; `rejects_modulo_by_zero`; `missing_or_non_numeric_expression_fields_do_not_match`; `rejects_numeric_expression_results_that_do_not_fit_json`; `rejects_unsupported_or_malformed_expr`; `rejects_malformed_json_query_corpus` | Boundary |
| QRY-003 | Physical cursors stop after the requested page and look-ahead record; emitted physical and sorted cursors bind to the table representation; sorted cursors enforce a matching keyset definition; bounded aggregation executes ordered input `$match`, top-level `$unwind` with its option form, one `$set` or `$addFields` stage with the shared bounded scalar expression subset, `$project`, `$sort`, `$skip`, and `$limit` stages, terminal `$group`, `$bucket`, bounded scalar-expression `$bucketAuto` with finite numeric results, or bounded scalar-expression `$sortByCount` stages with bounded accumulators, evaluates bounded numeric expressions for `$sum`, `$avg`, `$stdDevPop`, and `$stdDevSamp`, caps expansion and materialized values, and local joins enforce explicit bounds across chained stages. | `src/query/pagination.rs`; `src/dbf/types.rs`; `src/query/aggregation.rs`; `src/query/aggregation_plan.rs`; `src/query/aggregation_plan/bucket.rs`; `src/query/aggregation_plan/bucket_auto.rs`; `src/query/aggregation_plan/set.rs`; `src/query/aggregation_plan/types.rs`; `src/query/aggregation/standard_deviation.rs`; `src/query/aggregation/sort_by_count.rs`; `src/query/expression.rs`; `src/query/expression/numeric.rs`; `src/query/aggregation_tests/input_stage_tests.rs`; `src/query/aggregation_tests/accumulator_tests.rs`; `src/query/aggregation_tests/bucket_tests.rs`; `src/query/aggregation_tests/bucket_auto_tests.rs`; `src/query/aggregation_tests/sort_by_count_tests.rs`; `src/query/join/mod.rs`; `src/query/join/execute.rs`; `src/query/join_pipeline.rs`; `docs/aggregation.md`; `docs/joins.md` | `paginates_in_physical_record_order`; `rejects_a_physical_cursor_for_a_changed_snapshot`; `rejects_a_sorted_cursor_for_a_changed_snapshot`; `returns_distinct_values_after_matching`; `filters_group_output_before_projection_and_sorting`; `groups_filtered_records_with_count_and_integer_sum`; `groups_by_a_shared_scalar_expression`; `sums_fractional_and_integer_numbers`; `sums_numeric_literals_for_each_input_record`; `evaluates_bounded_numeric_accumulator_expressions`; `groups_numeric_average_and_returns_null_for_missing_values`; `groups_population_and_sample_standard_deviation`; `groups_comparable_extremes_and_returns_null_for_missing_values`; `groups_first_and_last_values_in_physical_order`; `groups_pushed_values_and_deduplicates_set_values`; `buckets_numeric_ranges_and_default_values`; `bucket_defaults_to_count_and_supports_group_output_stages`; `bucket_evaluates_a_shared_scalar_expression`; `rejects_invalid_bucket_boundaries_and_unmatched_values`; `bucket_auto_uses_sorted_numeric_ranges_and_default_count`; `bucket_auto_preserves_input_order_for_accumulators_and_output_stages`; `bucket_auto_emits_fewer_buckets_for_duplicate_values`; `bucket_auto_evaluates_a_shared_scalar_expression`; `bucket_auto_rejects_unsupported_or_non_numeric_input`; `sort_by_count_groups_missing_values_and_sorts_descending`; `sort_by_count_applies_input_and_group_output_stages`; `sort_by_count_evaluates_a_scalar_expression_per_record`; `rejects_unsupported_sort_by_count_forms_and_combinations`; `applies_input_stages_in_listed_order`; `projects_input_records_before_matching_and_grouping`; `sets_computed_fields_before_matching_and_grouping`; `sets_string_scalar_expressions_before_matching_and_grouping`; `add_fields_alias_sets_input_fields`; `unwinds_array_values_before_grouping`; `includes_unwind_array_indexes`; `preserves_null_and_empty_unwind_inputs_with_null_indexes`; `preserves_unwind_order_for_array_accumulators`; `filters_unwound_records_before_grouping`; `rejects_non_array_unwind_values`; `bounds_unwound_records`; `skips_sorted_group_output_before_limiting`; `bounds_materialized_group_values`; `chained_joins_reference_fields_from_prior_stages`; `right_join_keeps_unmatched_right_record`; `full_join_keeps_unmatched_left_and_right_records_in_direct_and_chained_queries`; and other join tests | Boundary |
| QRY-004 | Borrowed and owned-snapshot query streams apply filter, projection, skip, and limit incrementally and reject blocking or resumable controls. | `src/query/stream.rs`; `src/query/stream_tests.rs` | `streams_filtered_projected_records_with_bounded_controls`; `snapshot_stream_is_independent_of_later_table_mutations`; `streaming_rejects_blocking_and_resume_controls` | Boundary |
| QRY-005 | The documented Unicode-lowercase and Unicode-NFKC-lowercase sort collations apply their bounded string-key rules, make compatibility-equivalent CJK forms compare equal under NFKC, bind sorted cursors to the same collation, and avoid incompatible index ordering. | `src/query/ordering.rs`; `src/query/pagination.rs`; `src/query/planner.rs`; `src/query/tests.rs`; `src/query/cursor_tests.rs` | `supports_unicode_lowercase_collation_for_sort_keys`; `supports_unicode_nfkc_lowercase_collation_for_cjk_compatibility_keys`; `sorted_cursor_keeps_nfkc_collation_in_its_boundary`; planner collation fallback | Boundary |
| QRY-006 | The bounded snapshot stream runs a cloned table behind a positive-capacity standard-library channel, blocks the producer when the channel is full, preserves item order, and stops after consumer cancellation; asynchronous polling remains a separate boundary. | `src/query/stream.rs`; `src/query/stream_tests.rs`; `docs/query-model.md` | `bounded_snapshot_stream_keeps_a_fixed_snapshot`; `bounded_stream_requires_positive_capacity` | Boundary |
| QRY-007 | Equality joins select `NestedLoop` for at most 64 candidate pairs, then compare bounded hash and index-probe costs for direct and chained stages with exact pre-filter equality-key cardinality, materialized row-width work, logical input page reads, and logical index-sidecar page terms, with right-side outer-count reversal where required, while preserving the documented outer-row order and join semantics. Direct joins compare the compatible ordered-merge path, eligible chained non-`full` stages compare an ordered merge that includes intermediate sort work, and chained `full` stages retain the bounded hash fallback. | `src/query/join_strategy.rs`; `src/query/join_merge.rs`; `src/query/join_index.rs`; `src/query/join/mod.rs`; `src/query/join/execute.rs`; `src/query/join_pipeline.rs`; `src/query/join_pipeline/stages.rs`; `src/query/join_pipeline/stages/merge.rs`; `src/index/lookup/mod.rs`; `src/index/lookup/ordered.rs`; `docs/joins.md` | `chooses_nested_loop_for_small_join_inputs`; `chooses_hash_for_large_unindexed_inputs`; `chooses_index_nested_loop_when_the_outer_side_is_small`; `chooses_merge_for_large_dual_indexed_inputs`; `accounts_for_probe_page_reads`; `accounts_for_merge_index_pages`; `accounts_for_record_pages_and_materialization`; `accounts_for_sorting_the_intermediate_merge_input`; `indexes_include_the_outer_scan_pages`; `estimates_each_equality_join_shape`; `rejects_cross_join_cardinality_estimation`; `estimates_chained_stage_cardinality_from_qualified_keys`; `rejects_cross_stage_cardinality_estimation`; `scans_equal_key_runs_and_maps_them_to_outer_positions`; `large_join_ignores_a_stale_ordered_index_and_reads_new_rows`; `chained_join_uses_ordered_merge_for_a_small_intermediate_input`; `preserves_left_order_with_an_ordered_index`; `emits_right_major_rows_for_right_join`; large and chained indexed join cases; compound direct join cases; join output tests | Boundary |
| QRY-008 | `AsyncQueryStream` defines an executor-neutral `poll_next` contract with explicit ready, pending, end-of-stream, waker, and drop-cancellation semantics; borrowed and owned in-memory streams complete immediately without blocking, the bounded channel stream remains synchronous, and the native threaded adapter adds non-blocking polling, bounded backpressure, waker notification, and worker cancellation. | `src/query/stream_async.rs`; `src/query/stream.rs`; `src/query/stream_thread.rs`; `src/query/stream_tests.rs`; `docs/async-streaming.md`; `docs/query-model.md` | `in_memory_streams_implement_the_runtime_neutral_async_boundary`; `threaded_stream_polls_without_blocking_and_keeps_a_snapshot` | Boundary |
| QRY-009 | Path-aware single-table `QUERY /records` and `/explain` execution finishes normal DBF recovery, holds a shared table lock while reloading DBF, memo, and schema, and validates an index sidecar against that same table image; this protects txBASE readers that honor the lock but does not claim physical atomicity for legacy readers that ignore it. | `src/dbf/mod.rs`; `src/dbf/lock.rs`; `src/query.rs`; `src/query/planner.rs`; `src/server.rs`; `src/server/explain.rs`; `src/query/planner_tests.rs`; `docs/query-model.md`; `docs/xbf.md` | `consistent_path_queries_reload_the_table_used_by_the_index` | Boundary |
| CAT-001 | A catalog discovers only direct-child DBF files and reports table-specific DBF or index-sidecar verification failures. | `src/catalog.rs`; `src/catalog/tests.rs`; `docs/catalog.md` | `schema_and_verify_cover_every_discovered_table`; `verify_reports_a_malformed_table_by_name`; `verify_reports_a_stale_index_sidecar_by_table_name` | Current |
| CAT-002 | The catalog server exposes discovered schemas and bounded read-only joins while keeping cross-table mutation in the explicit transaction route. | `src/server/catalog.rs`; `docs/catalog.md`; `docs/http-semantics.md` | `catalog_server_query_join_executes_and_exposes_schema` | Boundary |
| CAT-003 | The catalog server exposes named-table GET, HEAD, QUERY, POST, PUT, PATCH, and DELETE routes while reusing single-table record, ETag, query, and mutation semantics. | `src/server/catalog.rs`; `src/server/records.rs`; `docs/catalog.md`; `docs/http-semantics.md` | `catalog_server_reads_named_tables_through_record_routes`; `catalog_server_mutates_named_tables_with_single_table_semantics` | Boundary |
| CAT-004 | The catalog server exposes the selected query plan for a named table through the same planner explanation contract as the single-table server. | `src/server/catalog.rs`; `src/server/explain.rs`; `docs/catalog.md`; `docs/http-semantics.md` | `catalog_server_reads_named_tables_through_record_routes` checks an index-backed `equality_index` plan | Boundary |
| CAT-005 | Named-table mutation operations can commit across multiple DBFs and their changed index sidecars through one catalog journal, roll back validation failures, and recover prepared or committed journal states, including index-sidecar targets. | `src/catalog/transaction.rs`; `src/catalog/journal_tests.rs`; `src/catalog/tests.rs`; `src/server/catalog_transaction.rs`; `docs/catalog.md`; `docs/http-semantics.md` | `commits_named_operations_across_tables`; `catalog_transaction_refreshes_indexes_for_each_changed_table`; `rejects_a_failed_named_transaction_without_persisting_earlier_tables`; `prepared_journal_rolls_back_partial_catalog_commit`; `committed_journal_replays_the_complete_catalog_commit`; `catalog_server_transaction_commits_multiple_named_tables`; `catalog_server_transaction_rolls_back_when_a_named_operation_fails` | Boundary |
| CAT-006 | Catalog-scoped scalar `references` and composite `constraints.foreign_keys` metadata validate non-null child values after named mutations and multi-table transactions, compare composite tuples positionally, and apply atomic restrict, cascade, or set-null actions to parent updates and logical deletes. | `src/catalog.rs`; `src/catalog/constraints.rs`; `src/catalog/transaction.rs`; `src/server/catalog.rs`; `src/server/records.rs`; `src/dbf/schema_metadata.rs`; `src/dbf/schema_metadata/constraints.rs` | `catalog_foreign_keys_validate_mutations_and_parent_removal`; `catalog_composite_foreign_keys_validate_tuples_and_parent_removal`; `catalog_foreign_key_actions_cascade_parent_changes_and_deletes`; `catalog_composite_foreign_key_actions_follow_parent_tuples`; `catalog_foreign_key_set_null_action_clears_children_atomically`; `catalog_server_rejects_orphan_foreign_key_mutations`; `composite_foreign_keys_validate_shape_and_local_fields`; `foreign_key_actions_require_references_and_nullable_children` | Boundary |
| CAT-007 | Multi-table catalog-journal commits persist a positive monotonically increasing ID, recover it with prepared/committed journal state, expose it in catalog schema JSON, and return it from `POST /transaction` in JSON and `X-Txbase-Transaction-Id`. | `src/catalog/journal.rs`; `src/catalog/journal_tests.rs`; `src/catalog/transaction.rs`; `src/catalog.rs`; `src/catalog/tests.rs`; `src/server/catalog_transaction.rs`; `docs/catalog.md`; `docs/http-semantics.md` | `commits_named_operations_across_tables`; `representation_tag_is_stable_and_changes_with_catalog_schema`; catalog journal recovery tests; catalog transaction HTTP test | Current |
| CAT-008 | Catalog-journal recovery applies or rolls back the catalog MVCC history together with DBF, memo, index, and transaction-state targets. | `src/catalog/journal.rs`; `src/catalog/journal_tests.rs`; `src/catalog/mvcc.rs`; `docs/catalog.md` | `prepared_journal_rolls_back_partial_catalog_commit`; `committed_journal_replays_the_complete_catalog_commit` | Current |
| CAT-009 | Explicit multi-table catalog commits publish one `TXCC` event containing each changed table's physical-record state diff, and catalog-journal recovery applies or rolls back the catalog CDC sidecar with the other commit targets. | `src/catalog/cdc.rs`; `src/catalog/transaction.rs`; `src/catalog/journal.rs`; `src/catalog/tests.rs`; `docs/change-data-capture.md` | `catalog_cdc_records_one_atomic_event_for_changed_tables`; `prepared_journal_rolls_back_partial_catalog_commit`; `committed_journal_replays_the_complete_catalog_commit` | Boundary |
| CAT-010 | Catalog HTTP read routes accept a positive committed catalog transaction ID through `at`, read every table from one retained catalog image, avoid current index sidecars for historical query and join execution, report a table scan for historical explain, and reject historical mutations. | `src/server/catalog.rs`; `src/server/explain.rs`; `src/server.rs`; `src/query/join/execute.rs`; `src/query/join_pipeline/stages.rs`; `src/server/catalog_tests.rs`; `docs/catalog.md`; `docs/http-semantics.md`; `docs/mvcc.md` | `catalog_server_reads_historical_snapshot_without_live_indexes` | Current |
| CAT-011 | `Catalog::begin_read` recovers pending state, captures every discovered table under the catalog and deterministic per-table read locks, releases locks after loading, preserves the captured catalog transaction ID, exposes read-only table copies and schema, and executes the bounded join contract without live index sidecars. | `src/catalog/read_transaction.rs`; `src/catalog.rs`; `src/dbf/lock.rs`; `src/dbf/mvcc.rs`; `src/query/join/execute.rs`; `src/query/join_pipeline.rs`; `src/query/join_pipeline/stages.rs`; `src/catalog/tests.rs`; `docs/catalog.md`; `docs/mvcc.md` | `catalog_read_transaction_keeps_a_read_only_cross_table_image` | Current |
| CAT-012 | `Catalog::begin_serializable` acquires the catalog write lock and every discovered table lock before recovery, captures the DBF name and file-name set, keeps the locks through commit, rollback, or drop, applies named record mutations to private table copies, validates the table set and loaded cross-table constraints, publishes one catalog journal transaction, and releases all locks afterward. | `src/catalog/serializable.rs`; `src/catalog/transaction.rs`; `src/catalog/constraints.rs`; `src/dbf/lock.rs`; `src/dbf/mod.rs`; `src/catalog/serializable_tests.rs`; `docs/catalog.md`; `docs/transactions.md`; `docs/mvcc.md` | `serializable_catalog_transaction_holds_all_locks_and_commits_once`; `serializable_catalog_transaction_rollback_releases_all_locks`; `serializable_catalog_transaction_rejects_table_set_change` | Boundary |
| SCH-001 | Optional schema metadata validates active records and mutation candidates, applies scalar defaults for omitted inserts, supports bounded composite primary/unique keys and table-level checks without changing legacy DBF bytes. | `src/dbf/schema_metadata.rs`; `src/dbf/schema_metadata/constraints.rs`; `src/dbf/mutation.rs`; `src/dbf/mutation_update.rs`; `src/dbf/schema_metadata_tests.rs` | `loads_schema_metadata_and_enforces_local_constraints`; `schema_defaults_fill_missing_insert_fields`; `composite_unique_constraints_reject_duplicate_keys`; `composite_primary_constraints_require_non_null_unique_keys`; `schema_checks_reject_invalid_candidates`; stale-sidecar and copy tests | Boundary |
| SCH-002 | `schema apply` validates a metadata candidate against the current DBF and active records under the table lock, atomically replaces only the schema sidecar, and leaves the DBF and transaction state unchanged on success or validation failure. | `src/dbf/mod.rs`; `src/dbf/schema_export.rs`; `src/cli/commands/inspect.rs`; `src/dbf/schema_metadata_tests.rs`; `tests/cli.rs`; `docs/schema-metadata.md` | `applies_valid_schema_metadata_without_rewriting_the_dbf`; `schema_apply_cli_installs_valid_metadata_without_changing_dbf_bytes` | Current |
| HTTP-001 | HTTP method discovery, JSON, JSON Merge Patch, and JSON Patch media-type, QUERY status, response-header, and update-operator boundaries are explicit. | `src/server.rs`; `src/server/records.rs`; `src/server/merge_patch.rs`; `src/server/json_patch.rs`; `src/server/transaction.rs`; `docs/http-semantics.md` | `src/server/tests/query_tests.rs::options_advertises_supported_methods_and_query_media_type`; `query_endpoint_enforces_json_boundary_and_executes`; `merge_patch_updates_known_fields_and_preserves_the_rest`; `merge_patch_recursively_merges_object_members`; `merge_patch_requires_an_object_root_and_patch_media_type`; `json_patch_updates_and_removes_fields`; `json_patch_rejects_invalid_operations_without_mutation`; mutation tests | Current |
| HTTP-002 | Supported single byte ranges and persistence failures return bounded responses without replacing the wrong table state. | `src/server/range.rs`; `src/server/tests.rs` | `query_endpoint_handles_single_byte_ranges`; `reloads_disk_state_after_persistence_failure` | Boundary |
| HTTP-003 | Successful table reads expose a strong ETag; state-changing routes honor optional If-Match and If-None-Match with their defined comparison, existing-resource wildcard semantics, 412 failure, no partial mutation, and a new ETag after commit. | `src/server.rs`; `src/server/records.rs`; `src/server/catalog.rs`; `src/server/transaction.rs`; `src/server/catalog_tests.rs`; `src/server/etag.rs`; `src/server/etag_tests.rs`; `docs/http-semantics.md` | `mutation_etag_prevents_lost_update`; `post_put_and_delete_honor_current_etag`; `if_match_requires_strong_tags_and_supports_existing_wildcard`; `mutation_if_none_match_rejects_current_representation_without_writing`; `transaction_if_none_match_rejects_current_representation_atomically`; `transaction_etag_precondition_is_atomic`; `catalog_named_table_routes_preserve_etag_preconditions` | Boundary |
| HTTP-004 | GET and HEAD routes honor If-None-Match with weak comparison, existing-resource wildcard semantics, 304 without a body, and the current ETag. | `src/server.rs`; `src/server/etag.rs`; `src/server/etag_tests.rs`; `docs/http-semantics.md` | `if_none_match_returns_not_modified_for_current_representation`; `head_reuses_record_headers_and_conditional_status` | Boundary |
| HTTP-005 | GET and HEAD record routes share status and representation headers; HEAD uses the same conditional ETag status without sending response content. | `src/server.rs`; `src/server/records.rs`; `src/server/etag_tests.rs`; `docs/http-semantics.md` | `head_reuses_record_headers_and_conditional_status` | Boundary |
| HTTP-006 | Catalog schema GET and HEAD routes expose a strong representation ETag and weak If-None-Match 304 behavior; catalog transactions evaluate If-Match and If-None-Match under the catalog write lock, return 412 without mutation on a failed condition, and return a new ETag after commit. | `src/catalog.rs`; `src/catalog/transaction.rs`; `src/server/catalog.rs`; `src/server/catalog_transaction.rs`; `src/server/etag.rs`; `src/server/tests.rs`; `docs/catalog.md`; `docs/http-semantics.md` | `catalog_etag_guards_schema_reads_and_transactions` | Boundary |
| HTTP-007 | Single-table and named-table stream routes accept JSON query documents with only filter, projection, skip, and limit, return one record per `application/x-ndjson` line without a content length, and use HTTP/1.1 chunked transfer with a bounded snapshot producer. | `src/server/stream.rs`; `src/server.rs`; `src/server/catalog.rs`; `docs/http-semantics.md` | `query_stream_endpoint_returns_chunked_ndjson`; `query_stream_endpoint_rejects_blocking_controls_before_streaming`; catalog stream route check | Current |
| HTTP-008 | Single-table and catalog `GET`/`HEAD /cdc` routes return bounded committed-event pages with an exclusive `after` cursor, a `next_after` continuation, a 1,000-event maximum, and `400` validation for invalid cursors or limits without acknowledging or retaining consumer state. | `src/server/cdc.rs`; `src/server.rs`; `src/server/catalog.rs`; `src/dbf/cdc.rs`; `src/catalog/cdc.rs`; `src/server/tests/cdc_tests.rs`; `docs/change-data-capture.md`; `docs/http-semantics.md` | `table_cdc_http_route_pages_committed_events`; `catalog_cdc_http_route_pages_atomic_events`; CDC page API tests | Current |
| CI-001 | Documentation translation structure and links, including the English/Japanese README heading structure, formatting, lint, and all-target tests are required on all supported CI operating systems. | `scripts/check-doc-translations.sh`; `README.md`; `README.ja.md`; `.github/workflows/ci.yml` | `bash scripts/check-doc-translations.sh`; GitHub Actions matrix run | Current |
| QRY-010 | Public query and join JSON parsers reject documents larger than the shared 1 MiB input limit before deserialization. | `src/lib.rs`; `src/query.rs`; `src/query/join/mod.rs`; `src/query/tests.rs`; `src/query/join_tests.rs`; `docs/query-model.md`; `docs/joins.md` | `rejects_a_query_document_over_the_shared_input_limit`; `rejects_a_join_document_over_the_shared_input_limit` | Boundary |
| WASM-002 | The host-independent WASM core rejects JSON inputs larger than the shared 1 MiB input limit before parsing and leaves its DBF snapshot unchanged. | `src/lib.rs`; `src/wasm.rs`; `docs/wasm.md` | `wasm_core_rejects_oversized_json_inputs_without_mutation` | Boundary |
| WASM-003 | The CI release artifact generated by `wasm-bindgen` can be loaded by the pinned Node.js host, reports ABI version 1, round-trips a DBF snapshot, exercises all four mutation methods, and verifies atomic batch rollback against the shared DBF core. | `Cargo.toml`; `.github/workflows/ci.yml`; `tests/wasm_smoke.mjs`; `docs/wasm.md` | `cargo build --locked --lib --target wasm32-unknown-unknown --release`; `wasm-bindgen --target nodejs`; `node tests/wasm_smoke.mjs target/wasm-bindgen` | Current |
| WASM-004 | The generated `wasm-bindgen` wrapper exposes a JavaScript host-backed asynchronous XBF object table whose five Promise-returning object-store methods preserve compare-and-swap publication, failed WAL cleanup recovery, historical reads, count-based retention, orphan cleanup, and tagged host-error mapping. | `src/wasm_edge.rs`; `src/edge/object_store_async.rs`; `src/edge/store.rs`; `.github/workflows/ci.yml`; `tests/wasm_edge_smoke.mjs`; `docs/wasm.md`; `docs/edge-storage.md` | `node tests/wasm_edge_smoke.mjs target/wasm-bindgen` | Boundary |
| HTTP-009 | HTTP JSON request bodies are limited to the shared 1 MiB input boundary and return `413 Payload Too Large` before parsing or persistence. | `src/lib.rs`; `src/server.rs`; `src/server/body.rs`; `src/server/tests/query_tests.rs`; `docs/http-semantics.md` | `query_endpoint_rejects_an_oversized_json_body_before_parsing` | Boundary |
| QRY-011 | `$expr` and input `$set`/`$addFields` share one scalar-expression AST and evaluator for field references, literals, null fallback, string concatenation, locale-independent Unicode case conversion, and bounded numeric expressions; incompatible values become non-matches or `null` according to the surface, and computed strings are capped at 1 MiB. | `src/query/expression.rs`; `src/query/expression/numeric.rs`; `src/query/aggregation_plan/set.rs`; `src/query/aggregation/input.rs`; `src/query/field_expression_tests.rs`; `src/query/aggregation_tests/input_stage_tests.rs`; `docs/query-model.md`; `docs/aggregation.md` | `compares_string_scalar_expression_results`; `string_scalar_expressions_support_null_fallback_and_literal_values`; `sets_string_scalar_expressions_before_matching_and_grouping`; `rejects_unsupported_or_malformed_expr` | Boundary |

## Gaps that remain explicit

The following topics have documentation or design notes but do not have a current implementation claim in the matrix:

- worker/WASI `AsyncQueryStream` timeout, transport, cancellation, and asynchronous-storage semantics; remote object-store adapters;
- filesystem- and cache-aware merge join strategies, streaming join execution, and aggregation stages or accumulators beyond bounded input `$match`, `$unwind`, `$set`/`$addFields` with its documented expression subset, `$project`, `$sort`, `$skip`, and `$limit`, group-output `$match`, `$count`, `$distinct`, `$group`, `$bucket`, bounded scalar-expression `$bucketAuto` with finite numeric results, and bounded scalar-expression `$sortByCount` with `$sum`, `$avg`, `$stdDevPop`, `$stdDevSamp`, `$min`, `$max`, `$first`, `$last`, `$push`, and `$addToSet`;
- predicate-level locking and distributed serializable coordination;
- locale-aware CJK collation and broader upstream CJK fixtures;
- Strict multi-file reader atomicity for schema-preserving XBF-to-DBF export, cloud object-storage adapters and retention policy, worker/WASI runtime adapters and asynchronous WASM storage, network transport, quorum or consensus, and distributed replication.

Before one of these becomes current, add its public contract, malformed-input behavior, crash or retry behavior, fixture or deterministic test, and a row here.

## Source basis

The quality model follows the categories described by SQLite's [testing overview](https://sqlite.org/testing.html), [quality-management plan](https://sqlite.org/qmplan.html), and [requirements catalog](https://sqlite.org/requirements.html).

Those sources motivate traceable requirements, independent malformed and fault tests, and reproducible release gates.

They do not imply that txBASE has SQLite's test volume, coverage, or release process.
